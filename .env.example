APP_NAME=IQO Strategy Lab
APP_ENV=development
APP_DEBUG=true

API_HOST=0.0.0.0
API_PORT=8000

DB_NAME=iqolab
DB_USER=iqolab
DB_PASSWORD=iqolab
DATABASE_URL=postgresql+psycopg://iqolab:iqolab@localhost:5432/iqolab

FRONTEND_URL=http://localhost:5173

LOG_LEVEL=INFO

# Sprint 7 - Live Execution Engine. Usadas apenas por Credentials.from_env()
# (app/execution/config.py). NUNCA commitar valores reais - preencha só no
# seu .env local, e apenas quando for validar manualmente em conta DEMO.
IQOPTION_EMAIL=
IQOPTION_PASSWORD=
